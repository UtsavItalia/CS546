<h1 class="search">{{title}}</h1>
<main>
  <div>
    <h1>
      {{name}}
    </h1>
    <img src="{{img}}" alt="{{name}} " />
    <p>
      {{description}}
    </p>
    <h2>Comics</h2>
    <ul>
      {{#each comics}}
        <li>{{this.name}}</li>
      {{/each}}
    </ul>
  </div>
</main>